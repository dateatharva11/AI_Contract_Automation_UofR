## Packages
date-fns | Date formatting
recharts | Dashboard charts and analytics visualization

## Notes
- Mock authentication is implemented via a top-bar Role Switcher (Admin, Reviewer, Vendor)
- AI endpoints (`generate-draft` and `analyze`) are expected to be long-running POST requests returning updated contract fields.
- Contract document uses a simplified text area editor bound to `documentContent` for MVP.
