Please make the following three modifications to my contract management application:

---

## MODIFICATION 1: ROLE-BASED ACCESS CONTROL

Currently, all users see all buttons. Implement proper role-based permissions:

- **Contract Manager role only** can see and access:
  - "Initiate Contract" button on dashboard
  - "New Contract" button on contracts page
  - "Add Vendor" button on vendors page
  - Edit/delete vendor information

- **All other roles** (Reviewers, Viewers) should NOT see these buttons or have access to these functions

Please add a user role field to the user model if not present, and ensure all UI elements check permissions before rendering.

---

## MODIFICATION 2: RICH TEXT EDITOR FOR CONTRACT DRAFTS

Replace the current plain text editor with a rich text editor for the contract draft window:

**Requirements:**
- After contract draft is generated, Contract Manager should see a full-featured rich text editor
- Must support Word document formatting (bold, italic, underline, bullets, numbered lists, headings)
- Table insertion and editing capability
- Track changes/version history visible
- Save button to persist edits
- Should maintain document structure when saving

Suggested libraries: TipTap, Quill, or Slate.js (choose whichever integrates best with current stack)

---

## MODIFICATION 3: REVIEWER SELECTION WORKFLOW

Create a reviewer selection step when submitting contracts for review:

**Database Requirements:**
- Add at least 5 sample reviewers with different roles:
  - Legal Counsel (2 people)
  - Facilities Manager
  - Sustainability Officer
  - Budget Office Representative
  - Risk Management

**UI Changes:**
- After contract draft is finalized, when Contract Manager clicks "Submit for Review"
- Show modal/page with multi-select dropdown of available reviewers
- Allow selecting multiple reviewers
- Each reviewer gets assigned to review specific sections (optional enhancement)
- Send notifications to selected reviewers

**Workflow:**
Step 1: Contract Manager creates contract → selects template → fills form → generates draft
Step 2: Edits draft in rich text editor (new feature)
Step 3: Clicks "Submit for Review" → opens reviewer selection
Step 4: Selects reviewers from list → confirms
Step 5: System assigns review tasks and notifies selected reviewers

---

Please implement these three modifications in priority order. Ensure all new features are demonstrable in the prototype with sample data.
